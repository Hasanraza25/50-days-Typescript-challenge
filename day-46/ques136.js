// Use console.log() to debug and track the value of a variable inside a loop.
var nums = [1, 2, 3, 4, 5];
for (var i = 0; i < nums.length; i++) {
    var currentVal = nums[i];
    console.log("Current Value at index ".concat(i, " is: ").concat(currentVal));
}
