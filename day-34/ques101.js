// Generate a random integer between 1 and 10.
function getRandomIntegar() {
    return Math.floor(Math.random() * 10 + 1);
}
var randomIntegar = getRandomIntegar();
console.log(randomIntegar);
// Output: Random integer between 1 and 10
